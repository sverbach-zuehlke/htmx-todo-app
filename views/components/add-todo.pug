form(class="flex flex-row gap-8 justify-between" hx-post="/todos" hx-target=".todos" hx-swap="afterend" hx-on::after-request="this.reset()")
    input(class="border border-black rounded-md px-2" placeholder="add new todo" name="name" required)
    button(type="submit")
        svg(xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6")
            path(stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15")
